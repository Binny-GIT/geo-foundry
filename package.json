{
  "name": "geo-foundry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": "11.22.0"
  },
  "exports": {
    "./package.json": "./package.json"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint . && turbo run lint",
    "typecheck": "tsc --noEmit --project tsconfig.base.json && turbo run typecheck",
    "test": "pnpm verify:toolchain && turbo run test",
    "test:fresh": "pnpm verify:toolchain && turbo run test --force --output-logs=full",
    "ci:verify": "node scripts/ci/verify.mjs",
    "test:ci-contracts": "node --test tooling/ci-contract.test.mjs",
    "test:workspace:fresh": "vitest run --config vitest.workspace.ts --configLoader runner --no-file-parallelism",
    "test:integration": "turbo run test:integration",
    "test:integration:fresh": "turbo run test:integration --force --output-logs=full",
    "test:serving-isolation": "pnpm build && pnpm --filter @geo/runtime test:integration",
    "test:faults:contracts": "node --test tests/faults/contracts.test.mjs && pnpm --filter @geo/domain exec vitest run test/site-host.test.ts && pnpm --filter @geo/compiler exec vitest run test/routes.test.ts test/two-sites.test.ts && pnpm --filter @geo/runtime test && pnpm --filter @geo/publisher exec vitest run test/artifact-store-adversarial.test.ts test/rollback.test.ts && pnpm --filter @geo/content-pipeline test && pnpm --filter @geo/quality-rules test && pnpm --filter @geo/worker test",
    "test:faults": "turbo run build --force --output-logs=full --filter=@geo/cms --filter=@geo/site-a-next --filter=@geo/site-b-express --filter=@geo/worker && node tests/faults/run.mjs",
    "test:e2e": "turbo run build --filter=@geo/site-a-next --filter=@geo/site-b-express && GEO_FOUNDRY_E2E_ENABLED=true pnpm exec playwright test",
    "test:e2e:update": "turbo run build --filter=@geo/site-a-next --filter=@geo/site-b-express && GEO_FOUNDRY_E2E_ENABLED=true pnpm exec playwright test --update-snapshots",
    "build": "turbo run build",
    "build:fresh": "turbo run build --force --output-logs=full",
    "check": "biome format . && pnpm lint && pnpm typecheck && pnpm test",
    "dev": "turbo run dev --parallel",
    "verify:toolchain": "node --test tooling/toolchain.test.mjs",
    "shared:check": "node scripts/shared-services/secure-run.mjs check",
    "shared:cleanup": "node scripts/shared-services/secure-run.mjs cleanup",
    "mvp:seed": "cd apps/cms && node scripts/secure-run.mjs node --import tsx scripts/mvp-seed.mjs",
    "mvp:run": "node scripts/mvp-scenario.mjs run"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@biomejs/biome": "2.5.8",
    "@geo/compiler": "workspace:*",
    "@geo/publisher": "workspace:*",
    "@geo/schema": "workspace:*",
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "esbuild": "0.28.2",
    "fast-check": "4.9.0",
    "turbo": "2.10.10",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1112.0",
    "pg": "8.16.3",
    "redis": "5.8.2",
    "zod": "4.1.12"
  }
}
