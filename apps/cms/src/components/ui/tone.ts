/**
 * Shared status vocabulary for the custom operations surfaces. Every tone
 * maps to the semantic --gf-tone-* pair in admin-theme.css, so the same
 * status always renders the same color no matter which page shows it.
 */
export type Tone = "accent" | "danger" | "neutral" | "success" | "warning"
